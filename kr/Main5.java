import java.util.*;

public class Main5 {
	public static boolean proverylkin(char [] array) {
		if (array[0] != '1' || array[0] != '2' || array[0] != '3') {
			return false;
		}
		if (array[])
	}
	public static void main(String[] args) {
		Scanner in = new Scanner(System.in);
		String line = in.nextLine();
		char [] array = Arrays.toCharArray(line);
		boolean flag = true;
		if (array[0] != '1' || array[0] != '2' || array[0] != '3') {

		}

	}
}